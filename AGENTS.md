# AGENTS.md — 「食觅」AI 开发上下文文件

> 所有人用 AI 工具（Cursor / Claude Code 等）改这个仓库代码前，先让 AI 读这个文件。
> 只改代码结构/数据结构/关键规则时才更新本文件，日常小修小补不用改。
> 完整设计细节看 `docs/项目规划书.md`，这里只放"AI 容易猜错、必须知道"的部分。

## 项目是什么

「食觅」：微信小程序，用户勾选**家里现有的食材+厨具+忌口**，反向匹配能做的菜（不是先想菜名再搜）。
当前阶段：V1，只做规则匹配，不接 AI/大模型，不做个性化推荐。

## 技术栈

- 前端：微信**原生小程序**（不是 uni-app），`pages/` 目录下按页面拆分。
- 后端：微信**云开发 CloudBase**，逻辑写在 `cloudfunctions/`，不建独立服务器，不用 NestJS/PostgreSQL。
- 数据库：云开发自带的文档型数据库（MongoDB 兼容）。
- 鉴权：微信 `openid`，云函数里用 `cloud.getWXContext()` 获取，不在前端存身份信息。

## 现有代码现状（fork 自 tonight-menu）

以下页面已有实际逻辑，**不是空壳，改动前先看懂现有代码再动**：

- `pages/fatloss`、`pages/ingredients`、`pages/recommendations`、`pages/home`、`pages/search`、`pages/profile`
- 云函数目前只有 `submitFeedback`，匹配/审核相关云函数需要新建。

> 三人分工进度请看 Git 协作规则文档，此处只记"哪些模块能复用、哪些要重写"，进度变化时更新这一段。

## V1 数据库字段（只列当前会用到的，预留字段见规划书，V1 阶段不要提前实现）

**dishes（菜谱，核心集合）**
`name`、`cover`、`totalTime`、`tags`、`ingredients[{name,num}]`、`tools[]`、`taboos[]`、`steps[{desc,time}]`、`openid`、`auditStatus`(0待审核/1已发布/2已驳回)、`rejectReason`

**ingredients**：`name`、`category`
**tools**：`name`
**collect**：`openid`、`dishId`
**audit_log**：`dishId`、`adminOpenid`、`action`、`reason`

⚠️ `dishes` 里还有 9 个预留字段（difficulty、season、stepImgs 等），**V1 不要用，不要提前建逻辑**，那是 V2/V3 的事。

## 核心匹配规则（改匹配逻辑前必须遵守）

1. 只查 `auditStatus=1` 的已发布菜谱，其他一律不能出现在公开查询结果里。
2. **厨具**：严格过滤，菜谱要求的厨具必须全部被用户勾选覆盖，缺一个就排除。
3. **食材**：宽松匹配，主要食材被覆盖即算命中，调料（盐糖油）不参与匹配。
4. **忌口**：严格排除，菜谱 `taboos` 和用户勾选忌口有交集就直接排除，不允许"大部分不含就行"。
5. 所有数据库读写走云函数，前端不直接查/改数据库。

## 投稿审核状态机

`0待审核 → 1已发布 / 2已驳回`，驳回可编辑重提交回 0。
只有投稿人本人和管理员能看到 0/2 状态的菜谱，其他人看不到。
管理员身份通过云函数里的 openid 白名单判断，目前只有 1 人是管理员。

## 环境与配置

- 三人共用一个云开发环境，环境 ID 不要硬编码提交到仓库里公开文件中。
- 测试数据用 `scripts/seed-dishes.js`（或类似初始化脚本）导入，别手动在控制台一条条加。

## 已知问题 / 踩过的坑

> 每次遇到花时间才解决的报错、坑点，简单记一笔在这，格式：日期 + 问题 + 解决方式。AI 改代码前先扫一眼，避免重复踩坑。

- （待补充）

## 更新这个文件的时机

只在以下情况更新，不用逐次改动都写：

- 数据库字段/集合结构变了
- 匹配规则、审核规则变了
- 目录结构大调整
- 踩了一个值得记录的坑
