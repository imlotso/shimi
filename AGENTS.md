# AGENTS.md — 「食觅」AI 开发上下文文件

> 所有人用 AI 工具改这个仓库代码前，先让 AI 读这个文件。
> 只改代码结构/数据结构/关键规则时才更新，日常小修小补不用改。

## 按角色看对应说明文件

本项目按角色拆了三份说明文件，**改对应模块代码前先读对应文件**，不要只看这一份就动手：

- 改小程序页面/UI/交互 → 先读 `docs/前端说明.md`
- 改云函数/匹配算法/审核逻辑 → 先读 `docs/云函数说明.md`
- 改数据库集合/初始化脚本/测试数据 → 先读 `docs/数据说明.md`

完整产品设计看 `docs/项目规划书.md`，协作流程看 `docs/Git协作规则.md`。本文件只放三个角色都需要知道的全局信息。

## 项目是什么

「食觅」：微信小程序，用户勾选**家里现有的食材+厨具+忌口**，反向匹配能做的菜（不是先想菜名再搜）。当前阶段：V1，只做规则匹配，不接 AI/大模型。

代码库 fork 自 `tonight-menu`（原项目名《今晚有谱》），原项目是个人练手项目，功能范围、数据结构都和「食觅」规划书不完全一致，改动前一定要先看对应角色说明文件里的"现状"部分。

## 技术栈

- 前端：微信**原生小程序**（不是 uni-app）
- 后端：微信**云开发 CloudBase**，不建独立服务器，不用 NestJS/PostgreSQL
- 数据库：云开发自带的文档型数据库（MongoDB 兼容）
- 鉴权：微信 `openid`，云函数里用 `cloud.getWXContext()` 获取

⚠️ `touristappid`（游客测试号）无法使用云开发/云函数/云数据库，必须用真实注册的小程序 AppID。

## 团队结构

4 人：BOBO 任仓库 admin + 代码审核人（不单独对应一个编码角色），另外 3 人分别对应前端/云函数/数据三个角色，具体分工及分支命名见 `docs/Git协作规则.md`。

## 全局红线（三个角色都要遵守）

1. 所有数据库读写走云函数，前端不直接查/改数据库。
2. 所有公开查询必须带 `auditStatus=1` 条件，未审核内容不能泄露。
3. `dishes` 集合里的 9 个预留字段（difficulty、season 等），V1 阶段不要用、不要提前建逻辑。
4. 环境 ID、AppID、管理员 openid 等不要硬编码提交进仓库。

## 已知问题 / 踩过的坑（跨角色通用的才写这里，具体角色的坑写到对应说明文件里）

- （待补充）

## 更新时机

数据库字段/集合、匹配规则、审核规则、目录结构发生大变化时更新。日常小改动更新到对应角色文件里就行。
